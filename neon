#include <iostream>
#include <cmath>
using namespace std;
int main () {
  int n,sq,sum=0;
  cin>>n;
  sq=pow(n,2);
  while(sq !=0) {
    sum +=sq%10;
    sq /=10;
  }
  if(sum==n) {
    cout<<n<<" is neon"<<endl;
  } else {
    cout<<n<<" is not neon"<<endl;
  }
  return 0;
}
